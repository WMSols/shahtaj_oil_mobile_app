import 'package:get/get.dart';

class RecoveryManRoutes {
  RecoveryManRoutes._();

  /// No pushed RM routes yet — shell embeds placeholder screens directly.
  /// Keep this list for future detail/create flows (e.g. handover detail push).
  static final pages = <GetPage>[];
}
